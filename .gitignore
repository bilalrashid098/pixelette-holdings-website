# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
build/
dist/

# Environment — never commit a value, only env.example with NAMES
.env
.env.local
.env.*.local

# Logs and caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
*.tsbuildinfo
next-env.d.ts

# Editor and OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Deployment
.vercel
.netlify

# Obsidian vault.
# /vault was the original location and no longer exists; the vault is /docs.
# These notes carry the legal, consent and evidence gates in plain language —
# s.21 FSMA positioning, unconfirmed certification wording, client consent that
# has not been obtained. This repository is PUBLIC. Both paths stay ignored.
/vault
/docs

# Palette comparison output (Phase B decision instrument, regenerated on demand)
.palette/
